import { redirect } from 'next/navigation'

/**
 * /services — redirects to the Services section on the homepage.
 * Replace with a dedicated services overview page when ready.
 */
export default function ServicesPage() {
  redirect('/#services')
}
